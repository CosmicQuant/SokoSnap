# Environment Variables Example
# Copy this file to .env.local and fill in the values

# API Configuration (Optional - for future backend integration)
VITE_API_URL=https://api.sokosnap.ke

# Feature Flags (Optional)
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_MOCK_DATA=true

# Note: All client-side environment variables must be prefixed with VITE_
# Server-only secrets should NOT be added here as they will be exposed to the client
